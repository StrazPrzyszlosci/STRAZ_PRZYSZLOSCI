# Resource Scout Cell

`resource_scout/` normalizes signals about free or low-cost resources so small
auditable automation cells can decide whether a resource is worth activating.

A resource record must include provenance, location, cost, resource class, possible
products, risks, required measurements/photos and activation requirements. The
cell is read-only/suggest-only: it records potential and blockers, not ownership
transfer or physical pickup approval.
